export enum Direction {
  CITY = 'city',
  COUNTRY = 'country',
}

export enum UserRole {
  PASSENGER = 'passenger',
  DRIVER = 'driver',
  BOTH = 'both',
}

export enum TripStatus {
  ACTIVE = 'active',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}

export enum WalletTransactionType {
  INITIAL = 'initial',
  DEBIT = 'debit',
  CREDIT = 'credit',
}
